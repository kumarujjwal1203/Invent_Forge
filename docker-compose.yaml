version: '3.9'

services:
  inventedge-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      INVENTEDGE_DATABASE_URL: file:inventedge.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
